{
  "name": "CalendarApp",
  "devDependencies": {
    "babel-cli": "^6.x.x",
    "babel-preset-es2015": "^6.x.x"
  },
  "scripts": {
    "build": "browserify -t [ babelify --presets [ react ] ] components/main_component.js -o bundle.js",
    "start": "browserify -t [ babelify --presets [ react ] ] components/main_component.js -o bundle.js"
  },
  "dependencies": {
    "dispatcher": "0.0.2",
    "invariant": "^2.2.1",
    "warning": "^2.1.0",
    "microevent": "1.0.0"
  }
}
